extends layout.pug

block content
    div
        article
            h1 Simple Metabase Dashboard Embed
            p This is a simple example of a static Metabase dashboard embedded in a web application.
            
            p The dashboard below is embedded using a signed JWT token that provides secure access to the specific dashboard without requiring user authentication.

            hr

            section
                h2 Embedded Dashboard
                p Below is the embedded Metabase dashboard:

                iframe(
                    src=iframeUrl
                    frameborder="0"
                    width="800"
                    height="600"
                    allowtransparency
                )